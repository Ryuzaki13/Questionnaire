<Page x:Class="Questionnaire.View.TeacherPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Questionnaire.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TeacherPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" Margin="10 10 5 10" ItemsSource="{Binding Questions}">
            
        </ListView>

        <Grid Grid.Column="1" Margin="5 10 10 10">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <ListView Grid.Row="0"></ListView>

            <StackPanel Grid.Row="1" Margin="0 10 0 0">
                <Button Content="Создать новую анкету" Click="onCreateNewForm"></Button>
            </StackPanel>
        </Grid>
        
    </Grid>
</Page>
